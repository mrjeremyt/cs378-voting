#include <iostream> // cin, cout

#include "Voting.h"

int main()
{
	using namespace std;
	int num_elections;
	cin >> num_elections;
	if (num_elections != 0)
		
	while (num_elections > 0)
	{
		run_elections(cin,cout);
		num_elections--;
	}
	return 0;
}